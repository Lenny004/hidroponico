services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: hidroponico
    ports:
      - "5435:5432"
    volumes:
      - hidroponico_pg:/var/lib/postgresql/data

volumes:
  hidroponico_pg:
